// Booleans

// true/false는 따로 따옴표를 붙이지 않는다. (파이썬은 True, False)

// null :  그 변수에 아무 값이 존재하지 않는다. (파이썬은 None)
// 즉, variable 안에 어떤 값이 없다는 것을 확실히 하기 위해 쓰는 것.
const choco = null; // choco라는 변수에 아무 것도 없는 상태로 채워 넣음. 

let something; // console.log 결과: undefined (메모리 공간에 존재, 값은 없음)